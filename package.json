{
  "name": "egt-delta-layers",
  "version": "1.5.0",
  "private": true,
  "description": "Blockbench plugin that keeps a texture's layer stack alive across saves by writing a sidecar next to the texture PNG.",
  "author": "Embody Games",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.com/Embody-Games/EGT-DeltaLayers.git"
  },
  "scripts": {
    "test": "node test/run_tests.js && node test/run_tests_52.js",
    "writes": "node test/count_writes.js",
    "release": "node scripts/release.mjs",
    "notes": "node scripts/release_notes.mjs",
    "icon": "node scripts/embed_icon.mjs"
  },
  "devDependencies": {
    "canvas": "^3.2.0"
  }
}
